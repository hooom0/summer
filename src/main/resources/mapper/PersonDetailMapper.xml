<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.PersonDetailMapper">

    <!-- 基本结果映射 -->
    <resultMap id="personDetailResultMap" type="com.example.demo.entity.PersonDetail">
        <id property="personId" column="person_id"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="idCard" column="id_card"/>
        <result property="phone" column="phone"/>
        <result property="position" column="position"/>
        <result property="status" column="status"/>
        <result property="registerTime" column="register_time"/>
        <result property="updateTime" column="update_time"/>

        <!-- 关联Person对象 -->
        <association property="person" column="person_id"
                     select="com.example.mapper.PersonMapper.selectById"
                     fetchType="lazy"/>
    </resultMap>

    <!-- 批量查询 -->
    <select id="selectBatch" resultMap="personDetailResultMap">
        SELECT * FROM person_detail
        WHERE person_id IN
        <foreach collection="personIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 条件查询示例 -->
    <select id="selectByCondition" parameterType="map" resultMap="personDetailResultMap">
        SELECT * FROM person_detail
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND gender = #{gender}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>

    <!-- 统计各状态人员数量 -->
    <select id="countByStatus" resultType="map">
        SELECT
            status,
            COUNT(*) AS count
        FROM person_detail
        GROUP BY status
    </select>
</mapper>